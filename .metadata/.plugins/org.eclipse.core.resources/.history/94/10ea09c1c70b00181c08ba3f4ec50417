package org.Refactoring;

public class PrintStatementFactory {
	public static PrintStatement getStatement(PrintType statementType, Customer customer) {
		switch (statementType) {
			case EMAIL:
				return new EmailStatement(customer);
			case PDF:
				return new PDFStatement(customer);
			default:
				break;
			}
			return null;
		}
	}
}
