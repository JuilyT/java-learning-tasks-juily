package org.Refactoring;

public interface PrintGenerator {
	/*
	 * We can pass type: pdf/email 
	 * msg: actual message
	 * accordingly we can implement Print()
	 */
	public void print(Customer cust, String msg);
}
